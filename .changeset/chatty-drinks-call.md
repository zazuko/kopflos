---
"@kopflos-cms/core": minor
---

All plugins are now added to `kopflos.config.(js|ts)` as now instances. Removed `PluginConfig` interface. 
Non-code configuration flavors still allow the plugin map object. 
